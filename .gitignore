# macOS
.DS_Store
.AppleDouble
.LSOverride

# Office temp files
~$*
*.tmp

# Secrets / env
.env
*.pem
.claude/settings.local.json

# Node (if ever needed)
node_modules/

# User context (template is tracked, local copy is not)
context.md

# Project subfolders (keep projects/ tracked via .gitkeep, ignore contents)
projects/*/
